# Required for GitHub write access
GITHUB_TOKEN=ghp_xxx
GITHUB_OWNER=your-org-or-user
GITHUB_REPO=your-repo

# Optional
GITHUB_BRANCH=main
FRONTEND_ORIGIN=http://localhost:5173
PORT=3001

# Optional comma-separated domain list used by the UI and backend validation
# Example: AVAILABLE_DOMAINS=forvaltning,bibliotek,jobcenter
AVAILABLE_DOMAINS=

# Optional currently active domain used by the frontend editor context
# Example: CURRENT_DOMAIN=forvaltning
CURRENT_DOMAIN=default

# Optional schema source overrides
# Defaults point to:
# https://github.com/OS2borgerPC/sikker-selvbetjening/tree/main/system_files/usr/share/sikker-selvbetjening/schemas
GITHUB_SCHEMA_OWNER=OS2borgerPC
GITHUB_SCHEMA_REPO=sikker-selvbetjening
GITHUB_SCHEMA_BRANCH=main
GITHUB_SCHEMA_PATH=system_files/usr/share/sikker-selvbetjening/schemas

# Optional UI schema source overrides (defaults to same folder/branch as schemas)
GITHUB_UI_SCHEMA_PATH=system_files/usr/share/sikker-selvbetjening/schemas
GROUPS_UI_SCHEMA_FILE=groups.uischema.json
BUILD_TARGETS_UI_SCHEMA_FILE=build_targets.uischema.json
